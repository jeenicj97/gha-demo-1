name: My GHA practise

on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build job
        run: echo "Yjos os  a nioj"
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
     - name: Test job
       run: echo "This is test job"
