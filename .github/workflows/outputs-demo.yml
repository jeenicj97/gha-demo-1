 name: Outputs Demo
 on: push
 jobs:
   Build:
     runs-on: ubuntu-latest
     steps:
       - name: Step One
         id: step1
         run: echo "version=0.0.1" >> $GITHUB_OUTPUT
       - name: Step Two
         run: "The version being used is ${{ steps.step1.outputs.version }}"
     
